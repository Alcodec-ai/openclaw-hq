services:
  dashboard:
    build: .
    image: openclaw-hq
    network_mode: host
    restart: unless-stopped
    environment:
      - OPENCLAW_HQ_PORT=7843
    volumes:
      - ~/.openclaw:/home/openclaw/.openclaw:rw
