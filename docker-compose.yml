services:
  dashboard:
    build: .
    image: openclaw-dashboard
    network_mode: host
    restart: unless-stopped
    environment:
      - OPENCLAW_DASH_PORT=7842
    volumes:
      - ~/.openclaw:/home/openclaw/.openclaw:rw
